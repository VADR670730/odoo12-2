<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="dezs.list">
      <field name="name">dezs list</field>
      <field name="model">dezs.dezs</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="dezs.action_window">
      <field name="name">dezs window</field>
      <field name="res_model">dezs.dezs</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="dezs.action_server">
      <field name="name">dezs server</field>
      <field name="model_id" ref="model_dezs_dezs"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="dezs" id="dezs.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="dezs.menu_1" parent="dezs.menu_root"/>
    <menuitem name="Menu 2" id="dezs.menu_2" parent="dezs.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="dezs.menu_1_list" parent="dezs.menu_1"
              action="dezs.action_window"/>
    <menuitem name="Server to list" id="dezs" parent="dezs.menu_2"
              action="dezs.action_server"/>
    -->
  </data>
</odoo>